# Vivo Lightroom Archive Converter v1.9.1

这是 v1.9 的拖放稳定性修复版本，视频编码、元信息保存和输出复检规则没有改变。

## 修复

- 修复拖入文件夹或异常 MP4 时，界面对缺失时长/帧率调用数值方法而直接崩溃的问题。
- 文件夹现在会自动展开第一层原始 MP4；已有的 `*_LR_*_archive.mp4` 和非 MP4 项目会被忽略。
- 输入检查限制为最多两个并发任务，避免一次拖入大量文件时启动过多 FFmpeg 进程。
- 输入无法读取时会显示“未知”并安全拒绝，不再导致整个 App 退出。

## 验证

- 通过 Computer Use 导入包含9个有效原片及已有档案输出的整个文件夹，9个原片均检查通过。
- 在同一文件夹加入故意损坏的 MP4 后，界面正确显示9个通过、1个拒绝，App保持运行。
- AppKit严格编译、应用构建、Info.plist与代码签名检查均通过。

依赖安装和已验证输入范围与 v1.9 相同。首次使用请运行压缩包内的 `install_dependencies.command`。
